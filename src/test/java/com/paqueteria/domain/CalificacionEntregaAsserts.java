package com.paqueteria.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class CalificacionEntregaAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCalificacionEntregaAllPropertiesEquals(CalificacionEntrega expected, CalificacionEntrega actual) {
        assertCalificacionEntregaAutoGeneratedPropertiesEquals(expected, actual);
        assertCalificacionEntregaAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCalificacionEntregaAllUpdatablePropertiesEquals(CalificacionEntrega expected, CalificacionEntrega actual) {
        assertCalificacionEntregaUpdatableFieldsEquals(expected, actual);
        assertCalificacionEntregaUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCalificacionEntregaAutoGeneratedPropertiesEquals(CalificacionEntrega expected, CalificacionEntrega actual) {
        assertThat(actual)
            .as("Verify CalificacionEntrega auto generated properties")
            .satisfies(a -> assertThat(a.getId()).as("check id").isEqualTo(expected.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCalificacionEntregaUpdatableFieldsEquals(CalificacionEntrega expected, CalificacionEntrega actual) {
        assertThat(actual)
            .as("Verify CalificacionEntrega relevant properties")
            .satisfies(a -> assertThat(a.getPuntaje()).as("check puntaje").isEqualTo(expected.getPuntaje()))
            .satisfies(a -> assertThat(a.getComentario()).as("check comentario").isEqualTo(expected.getComentario()))
            .satisfies(a -> assertThat(a.getFechaCalificacion()).as("check fechaCalificacion").isEqualTo(expected.getFechaCalificacion()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCalificacionEntregaUpdatableRelationshipsEquals(CalificacionEntrega expected, CalificacionEntrega actual) {
        assertThat(actual)
            .as("Verify CalificacionEntrega relationships")
            .satisfies(a -> assertThat(a.getPaquete()).as("check paquete").isEqualTo(expected.getPaquete()));
    }
}
